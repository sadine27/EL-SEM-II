# SP6 — CRM Minimal — Implementation Plan

**Date:** 2026-05-26
**Branch:** `claude/next-feature-implementation-0QYjJ`
**Design spec:** `docs/superpowers/specs/2026-05-26-sp6-crm-design.md`

---

## Task list

### Task 1 — Migration: three CRM tables
- File: `migrations/sp6/001_crm_tables.sql`
- Creates `private.suppliers`, `private.disputes`, `private.niche_performance`.
- Idempotent (`CREATE TABLE IF NOT EXISTS`).

### Task 2 — Supabase constants
- File: `el/supabase.py`
- Add table name constants: `CRM_SUPPLIERS_TABLE`, `CRM_DISPUTES_TABLE`, `CRM_NICHE_PERFORMANCE_TABLE`, `CRM_SCHEMA`.

### Task 3 — CRM data layer
- File: `el/crm.py`
- `list_niche_performance(db)` → `list[dict]`
- `list_suppliers(db)` → `list[dict]`
- `list_disputes(db, *, status=None)` → `list[dict]`
- `record_niche_run(niche, *, approved, rejected, avg_bcc_score, db)` → `dict`
  - Fetch current row → merge counters → upsert.

### Task 4 — `record_niche_performance` node
- File: `el/nodes/record_niche_performance.py`
- `run(ctx, *, db_provider=None)` → `ctx`
- Skip if niche absent; fail-soft on exception.

### Task 5 — Wire into pipeline
- File: `el/pipeline.py`
- Import `record_niche_performance`.
- Call at end of run, gated by `SUPABASE_URL` env var (same pattern as other Supabase nodes).

### Task 6 — CRM web route
- File: `el/web/routes/crm.py`
- `GET /crm` page (no auth).
- `GET /api/crm/niche-performance` (auth).
- `GET /api/crm/suppliers` (auth).
- `GET /api/crm/disputes` (auth).

### Task 7 — CRM template
- File: `el/web/templates/crm.html`
- HTMX shell; three `hx-get` sections that load on DOMContentLoaded.
- Minimal Tailwind table layout.

### Task 8 — Register router + update nav
- File: `el/web/app.py` — `include_router(crm_router)`.
- File: `el/web/templates/base.html` — add CRM link in nav.

### Task 9 — Tests
- `tests/test_crm.py`
- `tests/test_nodes_record_niche_performance.py`
- `tests/web/test_crm_routes.py`

### Task 10 — Update PHASE3_ROADMAP.md
- SP6 status → ✅, notes → delivered.
- Next action → SP8 (Docker + Hetzner deploy).
