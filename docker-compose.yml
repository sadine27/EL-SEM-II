services:
  api:
    image: ${EL_IMAGE:-el:local}
    env_file: ${EL_ENV_FILE:-.env}
    volumes:
      - el-data:/app/data
    ports:
      - "8000:8000"
    command: ["uvicorn", "el.web:create_app", "--factory", "--host", "0.0.0.0", "--port", "8000"]
    restart: unless-stopped
    stop_grace_period: 30s
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

  worker:
    image: ${EL_IMAGE:-el:local}
    env_file: ${EL_ENV_FILE:-.env}
    volumes:
      - el-data:/app/data
    command: ["python", "-m", "el.worker"]
    restart: unless-stopped
    stop_grace_period: 86400s
    deploy:
      replicas: 1

  caddy:
    image: caddy:2-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy

volumes:
  el-data: {}
  caddy-data: {}
  caddy-config: {}
