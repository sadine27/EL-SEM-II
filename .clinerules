# EL-SEM-II Project Memory — DIVYESH SHARMA — PROJECT DUE: JUNE 4, 2026 (TOMORROW)

## ⚡ CRITICAL: URGENCY LEVEL MAXIMUM
The project deadline is **TOMORROW (June 4, 2026)**. Every session must immediately resume work on the highest-priority unfinished items. Do NOT spend time re-exploring file structures or re-reading docs unless absolutely necessary. Jump straight into the next action.

## WHAT THIS PROJECT IS
- **EL Python Port**: Faithful Python port of n8n workflows (`legacy/EL.json`) for e-commerce product discovery + HIL (Human-in-the-Loop) pipeline
- **Phase 3 SaaS**: Full end-to-end SaaS build (SP1–SP9)
- **Stack**: Python 3.12, Supabase/PostgreSQL+pgvector, Vertex AI/Gemini, Docker, FastAPI, n8n
- **Deployed**: Docker Compose on laptop + Cloudflare Quick Tunnel. `/healthz` green.

## CURRENT STATUS — June 3, 2026

### ✅ MERGED TO MAIN (SP1–SP8):
| SP | Status | Key deliverable |
|----|--------|-----------------|
| SP1 | ✅ | Telemetry foundation (433 tests) |
| SP2 | ✅ | Source expansion |
| SP3 | ✅ | Vision + pgvector embeddings |
| SP4 | ✅ | FastAPI + RAG chat bot (555 tests) |
| SP5 | ✅ | Outbound: Gmail SMTP + Shopify auto-store (602 tests) |
| SP6 | ✅ | CRM minimal: supplier/dispute/niche tables + dashboard (661 tests) |
| SP8 | ✅ | Docker + Laptop deploy verified (652/652 green + 1 opt-in skipped) |

### ⬜ PENDING — SP7 (Paper Pipeline):
- **Blocked on**: ≥100 logged HIL events in `private.hil_logging_events`
- Branch: `feat/sp7-paper`
- Deliverables: `scripts/research/ips_overrides.py`, `scripts/research/override_descriptive.py`, `scripts/research/paper_figures.py`, `paper/phase3_overrides.tex`

### 🔴 SP9 — Forge + Sentinel HIL Provider (CURRENT PRIORITY):
- Migration exists: `migrations/sp9_allow_forge_sentinel_hil_provider.sql`
- This is the NEXT SUB-PROJECT in queue after SP8
- Design spec likely in `docs/superpowers/specs/` (check for SP9 or forge-related docs)

### 🟡 PENDING MIGRATIONS:
- `migrations/combined_apply_all.sql` — all SP1→SP3→SP4→SP6 migrations, idempotent
- `migrations/sp9_allow_forge_sentinel_hil_provider.sql` — SP9 migration
- Apply in Supabase SQL Editor, expose `private` schema in API Settings → Exposed schemas

### 🟡 PENDING PRODUCTION SMOKE:
- SP5 production smoke: Gmail app password + Shopify dev store + live send/upload
- POST /api/runs smoke (blocked on SP4 migration being applied in Supabase)

## PROJECT STRUCTURE KEY:
- `el/` — core Python package (pipeline, nodes, providers, web API)
- `legacy/` — original n8n workflows (source of truth)
- `migrations/` — SQL migrations for Supabase
- `scripts/` — build, calibration, verification, deploy scripts
- `tests/` — 652+ pytest tests, 90%+ coverage
- `docs/superpowers/specs/` — SP design specifications
- `docs/superpowers/plans/` — SP implementation plans
- `docs/runbooks/` — operational runbooks (deploy, shopify, etc)
- `paper/` — academic paper (LaTeX)

## COMMANDS:
```powershell
# Run tests
.venv\Scripts\python.exe -m pytest tests/ -q

# Run pipeline (dry-run)
.venv\Scripts\python.exe -m el run --dry-run

# Verify env
.venv\Scripts\python.exe scripts\verify_env.py

# Docker compose
docker compose up -d
```

## RULES FOR EVERY SESSION:
1. **Read the "Next action" line below FIRST** — it's the single most important thing to work on
2. Do NOT re-explore file structures — trust this memory file
3. Commits must stay granular (one per plan task), squash at merge
4. Every IO boundary must have fail-soft tests
5. New env vars must be documented in `.env.example`
6. Update this `.clinerules` file's "Next action" when completing a task

## NEXT ACTION (as of June 3, 2026 6:22 PM):
**SP9 — Forge + Sentinel HIL Provider completed ✅**:

  - [x] `normalize_sentinel_review.run(ctx)` wired into `pipeline.run()` after `sentinel_vetting`
  - [x] `merge_review_sources.run(ctx)` re-run after normalization so `sentinel_review_items` feed into `review_candidates`
  - [x] Pipeline-level integration test verifies Forge→Sentinel→HIL bridge end-to-end (10/10 passing)
  - [x] Full suite: 804 passed, 1 skipped (no regressions from 803)
  - [x] Migration exists: `migrations/sp9_allow_forge_sentinel_hil_provider.sql`
  - [x] CLI preview: `python -m el forge --query "RCB jersey" --json`

## PHASE 1 — ENABLE & MONITOR (IN PROGRESS)

### ✅ COMPLETED:
  - [x] 1.1 — SP9 migration applied in Supabase (`forge_sentinel` now allowed in hil_reviews)
  - [x] 1.2 — `private` schema noted as needing exposure in Supabase Dashboard → API Settings (can't set via SQL — user must check "private" in API Settings → Exposed Schemas)
  - [x] 1.3 — Sentinel monitoring: `crm.sentinel_summary()` implemented with pass rate, avg score, top rejection reasons, top sources, per-trend stats
  - [x] 1.4 — `GET /api/sentinel/summary` endpoint wired into FastAPI
  - [x] 1.5 — Sentinel telemetry logging: `_log_sentinel_decisions()` in `sentinel_vetting.py` logs every pass/reject to `private.sentinel_log` (table already exists)

### 🔴 NEXT — PHASE 2 (after user reviews):
  2.1 — Run pipeline live (EL_FORGE_PIPELINE_ENABLED defaults to `true`)
  2.2 — Monitor pass rates via /api/sentinel/summary
  2.3 — Tune thresholds after 10-20 real HIL responses
  2.4 — Gradually increase SENTINEL_PROVIDER_CAP

### 📋 PENDING USER ACTION:
  - Go to Supabase Dashboard → API Settings → Exposed Schemas and add `private` to the exposed schemas list (required for SP7 paper queries and sentinel_log access)

**Next: Wait for user to expose `private` schema in Supabase, then proceed to Phase 2 (live run)**

## REPO:
- GitHub: `https://github.com/sadine27/EL-SEM-II.git` (origin)
- Latest commit: `84b797ee2505917bcac407ed35044ea57a4e8528`