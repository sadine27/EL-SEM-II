# Runtime deps only. See requirements-dev.txt for tests/lint.
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
jinja2==3.1.4
python-multipart==0.0.12
httpx==0.27.2
requests==2.32.3
python-dotenv==1.0.1
google-auth==2.35.0
# google-auth signs service-account JWTs (RS256) via cryptography, whose OpenSSL
# bindings need the cffi backend. Pinned explicitly so the wheel + _cffi_backend
# are always present (otherwise Google/Vertex auth dies with ModuleNotFoundError:
# No module named '_cffi_backend').
cryptography>=42.0.0,<45
cffi>=1.16.0
sentry-sdk==2.18.0
cachetools==5.5.2
# Fenix engine — new data sources
# urllib3 must stay <2: pytrends 4.9.2 still calls Retry(method_whitelist=...),
# which urllib3 2.x removed (raises TypeError, so the pytrends source returns 0).
urllib3>=1.26.18,<2
pytrends>=4.9.2
newsapi-python>=0.2.7
beautifulsoup4>=4.12.3
lxml>=5.2.1
